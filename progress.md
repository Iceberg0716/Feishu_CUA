# Progress

Last updated: 2026-05-07

## Change Log
- 2026-05-07: Implement step-by-step LLM planning for `-i` natural language runs: `LLMPlanner.plan_intent` parses (product,intent,params), then `LLMPlanner.plan_next_subgoal` is called once per required composite subgoal (e.g. `im/send_message` => `open_chat` then `send_text`) and each subgoal is resolved/executed sequentially; added unit tests for the new step-by-step planner APIs. Files: `agent/llm_planner.py`, `runtime/natural_language_runner.py`, `tests/test_llm_planner.py`. Commands: `python -m compileall agent/llm_planner.py runtime/natural_language_runner.py`, `python -m unittest tests.test_llm_planner tests.test_plan_validator`.
- 2026-05-07: Revert `recorded.im.send_text_in_current_chat.v1` verification back to EXACT match and remove the extra input-box click step (return to the earlier behavior that sends whatever is in the input, e.g. `1HelloWorld`, without additional tolerance logic). File: `recorded_skills/im/send_text_in_current_chat.yaml`. Commands: `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`.
- 2026-05-07: Fix `python main.py -i "<instruction>"` IM send-message behavior: LLM planner prompt now requires IM composite intents to always plan `open_chat -> send_text`; PlanValidator enforces the exact subgoal sequence for `im/send_message` (and other IM composite intents) so plans that only open a chat are rejected; updated `recorded.im.send_text_in_current_chat.v1` VLM verification to accept messages that *contain* the target text (tolerate stray prefixes like `1HelloWorld`). Files: `agent/llm_planner.py`, `agent/plan_validator.py`, `recorded_skills/im/send_text_in_current_chat.yaml`, `tests/test_plan_validator.py`. Commands: `python -m unittest tests.test_plan_validator tests.test_llm_planner`, `python main.py -i "在测试群里发送 HelloWorld"`.
- 2026-05-07: Revert raising Ctrl+K top-region click guard for `vlm.find_chat_candidate` (restore min excluded height ratio to 0.14). Commands: `python -m compileall tools/semantic/vlm_find_chat_candidate.py`, `git diff --check`.
- 2026-05-07: Harden `vlm.find_chat_candidate` against clicking filter chips/search UI: compute a default `search_box_max_y` from config + screenshot height when not provided, strengthen VLM prompt to avoid UI chrome, and reject/snap click points that fall inside the excluded top region. Updated unit test to satisfy the new guard. Commands: `python -m compileall tools/semantic/vlm_find_chat_candidate.py providers/vlm_provider.py`, `python -m unittest tests.test_tools_vlm_find_chat_candidate tests.test_tools_vlm_judge_state tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `git diff --check`.
- 2026-05-07: Fix recorded IM open-chat click accuracy under DPI/window offset by introducing `gui.click_cropped_screenshot_point` (converts a click point from a window-cropped screenshot into absolute screen coords using `pywinauto` window rect). Updated `recorded.im.open_chat_by_search.v1` to crop search screenshot to Feishu window and use the new tool for clicking. Updated tests to include the new tool in the fake tool registry. Commands: `python -m compileall tools/gui/click_cropped_screenshot_point.py tools/defaults.py`, `python -m unittest tests.test_recorded_skill_executor tests.test_recorded_skill_loader tests.test_recorded_skill_registry`, `git diff --check`.
- 2026-05-07: Fix recorded IM open-chat click point mismatch: `recorded.im.open_chat_by_search.v1` now captures Ctrl+K search results as a full-screen screenshot (no window crop) so `vlm.find_chat_candidate` click_point coordinates align with `gui.click` screen coordinates; keeps window-cropped screenshot for post-click verification. Commands: `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `git diff --check`.
- 2026-05-07: Adjust `recorded.im.send_text_in_current_chat.v1` to avoid brittle input-box clicking: remove `gui.click_window_relative`, add `gui.focus_window` then type+enter, keep cropped screenshot + stricter VLM verification to reduce false SUCCESS. Updated composed-skill executor test to assert the send-text screenshot call instead of relying on `gui.click_window_relative`. Commands: `python -m unittest tests.test_recorded_skill_executor tests.test_recorded_skill_loader tests.test_recorded_skill_registry`, `git diff --check`.
- 2026-05-07: Tighten VLM verification prompt for `recorded.im.send_text_in_current_chat.v1` to avoid false SUCCESS: require exact message text to appear as a SENT chat bubble (not input draft), otherwise return success=false. Commands: `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `git diff --check`.
- 2026-05-07: Reduce VLM false positives by cropping post-send screenshots to Feishu window in `recorded.im.send_text_in_current_chat.v1` and `recorded.im.mention_member_in_current_chat.v1` (adds `title_keywords` + `crop_to_window` to `screen.screenshot` steps). Commands: `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `git diff --check`.
- 2026-05-07: Make `recorded.im.open_chat_by_search.v1` more reliable by explicitly focusing Feishu window first (`gui.focus_window`), cropping screenshots to the Feishu window, and sending `Esc` after clicking the candidate to close Ctrl+K overlay before verification. Updated unit test harness to register `gui.focus_window` in fake tool registry and adjust expected call order. Commands: `python -m unittest tests.test_recorded_skill_executor tests.test_recorded_skill_loader tests.test_recorded_skill_registry`, `git diff --check`.
- 2026-05-07: Make `-i` planning more robust: normalize LLM `status=ready` to `ok` in PlanValidator, and tighten LLM system prompt to require `status` in {ok,missing_capability,clarification_required,missing_params} and non-empty `subgoals` when ok. Commands: `python -m compileall agent/llm_planner.py agent/plan_validator.py`, `python -m unittest tests.test_plan_validator`, `git diff --check`.
- 2026-05-07: Tighten LLM planner prompt to forbid extra keys like `skill_id/recorded_skill_id/reason`, reducing `Result: REJECTED` due to harmless schema drift. Commands: `python -m compileall agent/llm_planner.py`.
- 2026-05-07: Improve `-i` failure diagnostics: when natural language planning is rejected/missing params/capability, CLI prints `Error:` unconditionally and dumps a small JSON debug payload (`llm_plan` + validation result). Commands: (no new commands).
- 2026-05-07: Improve recorded IM macros reliability by adding VLM-based verification steps: `recorded.im.open_chat_by_search.v1` now screenshots after click and uses `vlm.judge_state` to confirm the opened chat matches `chat_name`; `recorded.im.mention_member_in_current_chat.v1` now screenshots after send and uses `vlm.judge_state` to confirm the mention message was actually sent (avoid false SUCCESS when only input box draft exists). Also updated LLM planner prompt to explicitly prefer `open_chat` subgoals for IM (recorded skills may use Ctrl+K internally). Commands: `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `python -m unittest tests.test_llm_planner tests.test_plan_validator`, `git diff --check`.
- 2026-05-07: Add natural language shortcut `python main.py -i "<instruction>"` that uses a text LLM semantic planner (JSON-only: product/intent/params/subgoals) + PlanValidator to resolve subgoals into compatible recorded skills, then executes them sequentially via RecordedSkillExecutor. Added `agent/llm_planner.py`, `agent/plan_validator.py`, `runtime/natural_language_runner.py`, `providers/text_llm_provider.py`, and unit tests `tests/test_llm_planner.py` + `tests/test_plan_validator.py`. Updated `.env.example` + `config.example.yaml` with `LLM_BASE_URL/LLM_MODEL/LLM_API_KEY`. Commands: `python -m compileall .`, `python -m unittest tests.test_llm_planner tests.test_plan_validator tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `python -m unittest tests.test_plan_validator`, `git diff --check`, `python -c "import runtime.cli; print('cli_import_ok')"`.
- 2026-05-06: Make OCR optional via `vision.ocr_enabled`; when disabled, OCR tools/providers are not initialized/called and `im.search_chat` runs VLM-only; `doctor` prints OCR import failures as WARN and skips checks when OCR is disabled.
- 2026-05-06: Tune VLM-only Ctrl+K click guard by introducing `im.search_box_region_max_y_ratio` and pass it into `vlm.find_chat_candidate` as `search_box_max_y` to avoid false failures on top results. Commands: `python -m compileall .`, `git diff --check`.
- 2026-05-06: Add sidecar “Recorded Skill” subsystem (YAML macros with metadata/pre/postconditions, loader/registry/template renderer/executor, and `python main.py recorded ...` CLI). Baseline `im.send_message` path unchanged. Commands: `python -m compileall .`, `python -m compileall runtime/cli.py`, `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor`, `git diff --check`.
- 2026-05-06: Add recorded subgoal planner for reuse + `missing_capability` reporting; enhance `python main.py recorded plan` to output JSON and support `--product/--intent`; add `tests.test_recorded_planner`; update `docs/recorded_skills.md`. Commands: `python main.py recorded plan --product im --intent send_emoji --param chat_name=测试群 --param emoji_name=笑脸 --state feishu_window_available`, `python -m compileall .`, `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor tests.test_recorded_planner`, `git diff --check`.
- 2026-05-06: Add recorded IM macro `recorded.im.mention_member_in_current_chat.v1` (YAML only) and extend RecordedPlanner to support `intent=mention_member` (open_chat + mention_member) with clearer `missing_params` reporting. Tests updated. Commands: `python main.py recorded plan --product im --intent mention_member --param chat_name=测试群 --param member_name=张三 --param message=请看一下 --state feishu_window_available`, `python -m compileall .`, `python -m unittest tests.test_recorded_skill_loader tests.test_recorded_skill_registry tests.test_recorded_skill_executor tests.test_recorded_planner`, `git diff --check`.

用于新会话快速了解“当前做到哪一步”。详细历史以 `git log` 为准（不再在此文件里堆叠长日志）。更新规范见 `AGENTS.md` 的 Progress Hook。

## 当前状态（截至 2026-05-06）

- 仓库：`d:\\Code\\Python-Learning\\CUA_new`
- MVP 骨架：已完成（Skills/Tools/Runner/Reports/单测齐全）
- IM 打开会话：已改为 **OCR/VLM 点击候选**（不再用 Enter/Down+Enter 打开会话）

## IM 会话打开策略（现状）

- 流程：`app.open_or_focus` → `Ctrl+K` → `Ctrl+A` → 粘贴 `chat_name` → 截图（裁剪到飞书窗口）→ OCR 选候选（不明确则 VLM JSON 兜底）→ 点击候选 → `verify_chat_opened`
- 关键 evidence：`open_strategy:ocr_vlm_click`、`screenshot:search_results:...`、`ocr_candidate_count:*`、`selected_candidate_source:*`、`selected_candidate_bbox:*`、`click:x,y`、`verify_chat_opened:true/false`

## 当前阻塞（需要优先解决）

- OCR 引擎在 Windows 环境崩溃，导致 `vision.ocr_extract` 失败，后续“选候选/点击/verify”都不会发生。
- 最新复现 run：`artifacts/runs/2026-05-06_214143/logs/run.jsonl`
  - 报错：`ocr_extract failed: (Unimplemented) ConvertPirAttribute2RuntimeAttribute ... onednn_instruction.cc:118`
  - 截图：`artifacts/runs/2026-05-06_214143/screenshots/search_results_20260506_214155_520631.png`（候选非常清晰，但 OCR 没跑起来）
- 环境信息（本机）：Python `3.13.7`，Paddle `3.3.1`

## 相关配置（常用项）

- `im.open_chat_strategy`: `"ocr_vlm_click"`
- `im.search_chat_max_retries`: `2`
- `im.after_ctrl_k_wait_seconds` / `im.after_ctrl_a_wait_seconds` / `im.after_paste_chat_name_wait_seconds` / `im.search_results_wait_seconds` / `im.after_open_chat_wait_seconds`
- `im.ocr_candidate_min_confidence` / `im.ocr_candidate_min_score`

## 下一步建议（讨论后再动手）

- 先解决 OCR 环境/依赖崩溃（否则 IM/verify 相关 OCR 全都会失败）。
- 再考虑是否允许 “OCR 失败时直接走 VLM-only 兜底”（属于策略变更）。
