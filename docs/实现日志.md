# 实现日志

## 2026-04-30

### 基线

- 修改前已保存本地 git 基线提交：`a351e82`
- 提交信息：`chore: save pre-integration baseline`

### 已落地的集成方向

- 借鉴 ShowUI / ShowUI-pi：
  - 前台窗口裁剪
  - 分区分析
  - 二阶段定位
- 借鉴 LLaVA-VLA：
  - 动作 schema
  - `ActionChunk`
  - 多步动作片段执行

### 本轮实现：模板前后条件

- 重写 `knowledge/feishu.json`
  - 修复原文件中的异常内容和编码污染
  - 为模板增加 `preconditions`
  - 为模板增加 `postconditions`
- 修改 `cua_lark/orchestrator.py`
  - 增加 `_template_preconditions_met()`
  - 增加 `_template_postconditions_met()`
  - 增加 `_plan_vlm_action()`
  - 模板前置条件不满足时，直接不命中模板
  - 模板后置条件失败时，先恢复，再重新规划 VLM 动作
- 修改 `tests/test_runtime_no_model.py`
  - 保留原有 3 个场景
  - 新增“前置条件失败回退”场景
  - 新增“后置条件失败回退”场景

### 本轮验证

- 无模型闭环测试通过：
  - `D:\\python\\python310\\python.exe tests\\test_runtime_no_model.py`
- 语法校验通过：
  - `cua_lark/orchestrator.py`
  - `cua_lark/knowledge_base.py`
  - `tests/test_runtime_no_model.py`

### 结果

- 模板系统现在不再只是“命中就执行”。
- 模板执行链路现在具备：
  - 命中前检查
  - 执行后校验
  - 失败后回退

### 本轮实现：补测试

- 新增 `tests/test_parser.py`
  - 验证 fenced JSON 提取
  - 验证坐标裁剪
  - 验证热键字符串解析
  - 验证 `ActionChunk` 标志位和拖拽动作解析
- 新增 `tests/test_knowledge_and_screenshot.py`
  - 验证知识库能读取模板前后条件
  - 验证前台窗口裁剪
  - 验证分区布局
  - 验证 `mark_step()` 删除截图并更新 `index.json`
- 新增 `tests/test_recovery.py`
  - 验证已在前台时的聚焦结果
  - 验证状态入口动作来自知识库
  - 验证缺失恢复策略时返回失败
  - 验证全局恢复动作和状态入口动作的顺序
- 运行结果：
  - `python -m unittest tests.test_parser tests.test_knowledge_and_screenshot tests.test_recovery` 通过
  - `tests/test_runtime_no_model.py` 再次通过

### 本轮实现：Bug 修复合集

- 修复 4 个 trace 分析发现的问题：
  - `parser.py`：滚动参数按 `dy` → `pixels` → `amount` → `direction` 顺序解析
  - `orchestrator.py`：`_execute_action` 增加 `is_template` 参数，保留模板原有标志位
  - `vlm_client.py`：SYSTEM_PROMPT 增强，输入类指令强制用 ActionChunk 格式（先 click 再 type）
  - `orchestrator.py`：新增 `_offset_action_coords`，`WindowBounds` 窗口坐标 → 屏幕绝对坐标

### 本轮实现：VLM 小图像保护

- `screenshot.py`：`crop_foreground_window` 窗口 <20px 时回退全屏
- `vlm_client.py`：3 个 VLM 函数增加 `VLM_MIN_DIM = 20` 尺寸检查，<20px 返回安全默认值

### 本轮实现：定位准确性增强

- `config.py`：新增 `localization_mode`（full_window/region）和 `preclick_confirmation` 配置项
- `orchestrator.py`：`_plan_vlm_action` 实现 full_window 模式；`_confirm_click_target` 实现 80x80 补丁 VLM 二次确认
- `vlm_client.py`：新增 `confirm_click_target` 函数及 `CONFIRM_TARGET_PROMPT`

### 本轮实现：滚动与 ActionChunk 确认修复

- `action_types.py`：`ScrollAction` 增加 `x`/`y` 字段
- `parser.py`：滚动解析同时提取 `x`/`y`
- `operator.py`：`execute_scroll` 先 `moveTo(x, y)` 再 `scroll(dy)`
- `orchestrator.py`：`_offset_action_coords` 和 `_scale_atomic` 处理 ScrollAction 坐标
- `orchestrator.py`：`_confirm_click_target` 递归处理 ActionChunk，取第一个 click 子动作确认
- `orchestrator.py`：template fallback 分支增加 `_confirm_click_target` 调用
- `tests/test_runtime_no_model.py`：新增 `confirm_click_target` mock，更新所有 `with` 上下文

### 本轮实现：中文输入支持

- `operator.py`：`execute_type` 对非 ASCII 文本用 `pyperclip.copy() + Ctrl+V` 粘贴，粘贴前 `Ctrl+A → Backspace` 清空输入框
- `requirements.txt`：新增 `pyperclip>=1.8.0`

### 本轮验证

- 单元测试 13 项全部通过
- `tests/test_runtime_no_model.py` 5 场景全部通过
- `py_compile` 全部通过
